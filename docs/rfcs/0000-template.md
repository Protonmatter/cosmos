---
rfc: 0000
title: <short descriptive title>
status: Draft
author: <name or handle>
created: <YYYY-MM-DD>
updated: <YYYY-MM-DD>
supersedes: —
superseded-by: —
specs: <spec files this RFC creates or changes, or "—">
---

# RFC 0000 — <title>

> Copy this file to `docs/rfcs/NNNN-short-slug.md`, taking the next free number.
> Delete the guidance blockquotes as you write.

## Summary

> One paragraph. What changes, in plain language. A reader should be able to stop
> here and still know what was decided.

## Motivation

> What problem does this solve? What is broken, risky, or unclear today? Prefer
> concrete evidence — a bug that shipped, a claim that drifted out of date, a
> manual step someone keeps forgetting.

## Guide-level explanation

> Explain the change as you would to a contributor who will use it. Show the
> shape of the thing: file layout, commands, examples. No internals yet.

## Reference-level explanation

> The precise design. Interfaces, file formats, selectors, invariants. This is
> the section the specification is derived from.

## Normative requirements

> The testable obligations this RFC introduces, each with a stable identifier.
> Use RFC 2119 / RFC 8174 keywords (MUST, SHOULD, MAY). Every MUST-level
> requirement must be traceable to at least one automated test before the RFC
> can move to Accepted — `npm run test:trace` enforces this.
>
> Requirements live in the specification files under `docs/specs/`; list the
> identifiers introduced here so reviewers can find them.

| ID | Level | Requirement | Spec |
| --- | --- | --- | --- |
| `AREA-001` | MUST | … | `docs/specs/<file>.spec.md` |

## Drawbacks

> Why might we not want to do this? Cost, complexity, maintenance burden.

## Alternatives considered

> What else was on the table, and why this option won. "Do nothing" is always an
> alternative worth stating.

## Prior art

> How comparable projects solve it, and what we take or reject from them.

## Unresolved questions

> What is deliberately left open, and who decides it later.

## Rollout and migration

> How this lands: in one change or in stages, what has to be updated in step,
> and how it can be reverted if it goes wrong.
